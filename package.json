{
  "name": "author-books",
  "private": true,
  "scripts": {
    "build:web": "pnpm --filter frontend typecheck",
    "build:api": "tsc -p apps/api/tsconfig.json"
  },
  "workspaces": [
    "packages/*",
    "apps/*",
    "frontend"
  ],
  "devDependencies": {
    "typescript": "^5.6.0",
    "ts-node": "^10.9.1"
  }
}
