{
  "name": "author-books",
  "private": true,
  "scripts": {
    "build:web": "tsc -p apps/web/tsconfig.json",
    "build:api": "tsc -p apps/api/tsconfig.json"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "typescript": "^5.6.0",
    "ts-node": "^10.9.1"
  }
}
